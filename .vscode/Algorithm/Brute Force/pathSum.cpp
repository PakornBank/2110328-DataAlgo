#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int k[8];
    for (auto& i : k) {
        int input;
        cin >> input;
        i = input;
    }
    for (int i = 0; i < n; ++i) {
        int a, b, w;
        cin >> a >> b >> w;
    }
}
